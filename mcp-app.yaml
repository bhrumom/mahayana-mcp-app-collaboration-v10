schemaVersion: 3
pluginId: io.mahayana.test.github-native-collaboration
version: 1.0.1
source:
  provider: github
  defaultBranch: main
  licenseSpdx: Apache-2.0
extension: io.modelcontextprotocol/ui
mcpAppsVersion: 2026-01-26
ui:
  resource: ui://io.mahayana.test.github-native-collaboration/main
  mimeType: text/html;profile=mcp-app
  displayModes: [inline, fullscreen]
toolContract: tool-contract.json
permissions: permissions.json
artifacts:
  common:
    path: common
  native-cli:
    targets:
      - { os: macos, architecture: arm64 }
      - { os: macos, architecture: x64 }
      - { os: windows, architecture: x64 }
      - { os: linux, architecture: x64 }
      - { os: linux, architecture: arm64 }
  web-wasm:
    targets: [ios, android, desktop-webview, web, pwa]
    isolation: worker
